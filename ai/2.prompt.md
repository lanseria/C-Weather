### history

继续给 app/components/WeatherHourly.vue 代码中，优化一下细节
现在渐变都在上部分，导致被遮罩之后没有很明显的颜色了。应该
渐变从文字图标顶部开始，再往上是对应天气的各种颜色 比如： orange-100/50 sky-100/50...

### history

继续给 app/components/WeatherHourly.vue 代码中，优化一下细节
背景填充元素以及里面的icon与文字都在温度曲线下方，并保证曲线留有一定的高度保证。可以考虑遮罩功能，曲线上方为透明，下面为背景填充

### history

给 app/components/WeatherHourly.vue 代码中，优化一下细节
1. 给每个 weatherGroups 展示天气图标的区域，添加背景元素比如
  晴天是谈谈的橙色渐变
  阴天时灰色
  雨天是模仿水滴，比如30度的细虚线
  下雪则是冰蓝色
  多云则是浅蓝色

### history

app/components/WeatherDaily.vue 与 app/components/WeatherCurrent.vue 中的仅展示天气图标太简单了，也添加上天气文字描述

### history

优化 hourlyData 中的时间展示，不需要每个时刻都展示，可以仅展示 0点，4，8，12，16，20，这样每隔4小时展示一个点，这样更清晰。同时需要展示“现在”这个时间点

### history

我希望常量配置中的
COLUMN_WIDTH设置同时可以影响到指示器位置。这样不用单独修改两次了，
同样的 const CHART_HEIGHT = 176 也是，与下面的 h-[176px] 一致

### history

使用虚线展示过去一天的温度曲线图，现在是实线展示温度曲线图，也就是虚线向右平移一天

### history

给 server/utils/weather.ts 添加上获取过去一天的参数，除了可以用来返回，也可以用来进行对今日温度的对比（记得 app/components/WeatherHourly.vue 添加上判断）

### history

现在补充上温度单位的设置
摄氏度 (Celsius, °C)
华氏度 (Fahrenheit, °F)
开尔文 (Kelvin, K)

### history

我希望在合适的地方添加一个配置，打开配置弹窗，可以配置
全局
比如的
风力(包含阵风)单位：蒲福风级 (Beaufort Scale)，速度物理单位（米每秒，千米每小时，节，英里每小时）
其他的比如温度单位/空气质量（以后做）


### history

我希望在空气质量与风力做一下区分。
我希望空气质量的数值 aqiGroups 不要分组，就每个小时展示一个方块，同时
当 指示器的线在对应的空气质量方块时高亮。


### history

分别给 温度，空气，风力对应的行添加上 label 放在其对应的左侧，垂直居中展示

### history

再优化一下
<path :d="chartData.areaPath" fill="url(#tempGradient)" />
              <path
                :d="chartData.linePath"
                fill="none"
                stroke="#10b981"
                stroke-width="3"
                stroke-linecap="round"
                stroke-linejoin="round"
              />
中 areaPath与linePath的计算，曲线与背景现在是当前小时的中间开始画的，我希望是占满第一个小时

### history

优化一下 \app\components\WeatherHourly.vue 组件中滚动，
我想要在 web 端时，可以通过滚动控制 x 轴滚动，同时在 移动端时，应该可以左右滑动

### history

<div class="pt-2 flex w-full pointer-events-none left-0 top-0 absolute z-10">
              <div
                v-for="group in weatherGroups"
                :key="group.id"
                class="border-r border-gray-100/50 flex flex-col h-full items-center justify-start last:border-0 dark:border-gray-700/50"
                :style="{ width: `${group.count * COLUMN_WIDTH}px` }"
              >
                <div class="mt-1 opacity-80 flex flex-col gap-1 items-center">
                  <span class="text-[11px] text-gray-500 font-medium dark:text-gray-400">
                    {{ getWeatherName(group.data.code) }}
                  </span>
                  <div :class="getWeatherIcon(group.data.code, 1)" class="text-xl text-gray-500 dark:text-gray-400" />
                </div>
              </div>
            </div>

我希望这部分内容放在 svg 温度曲线下方，两边的 border 与 svg温度曲线高度一致

### history

修复一下 Marker 指示器层，先做成固定在整个容器的左侧，容器内容的元素可以 x 轴移动，但在滚动时，Marker 指示器层始终在左侧，与其固定的圆圈 (指示温度点) 会上下随图表移动，并展示 tooltip展示具体的温度数值。

### history

我希望不使用 echarts 绘制图表，而是纯 svg 来实现这个需求，这样更加灵活

### history

应该将 left与 right 设置为 0 这样，就不会有左右 padding了。
然后添加上一个 标志线 用来在滚动时查看对应的温度，默认这个标志线在最左边，当滚动时，标志线会自动移动到，滚到最后标志线会在最右边。类似图中所示

### history

我希望这几内容，如果他们数据一致，相同，就在 UI 进行合并，比如天气，空气质量，风力风向

### history

然后将这逐小时预报展示，模仿图片的形式，下方是时间展示，从下到上是风力，空气，然后是气温与天气的结合曲线图表

### history

优化一下 app/components/WeatherHourly.vue 中
不要只取未来24小时，尽可能展示每个小时的天气信息

### history

我希望将 server/routes/api/weather.get.ts 的请求接口做成一个 utils ,这样mcp与它使用同一个方法即可

### history

将 server/mcp/tools/weather.ts 的请求接口与 server/routes/api/weather.get.ts 一致，支持空气质量/按小时/按天查询等功能
同时，将请求 api.open-meteo.com 都添加一个代理配置，NUXT_PROXY_URL，支持使用代理去请求接口

### history

先完成一个天气纯前端应用
展示
当前的
天气
空气质量
温度
湿度
风力风向
体感温度

逐小时的
天气
温度
空气质量
风力风向

每日的
总体天气
平均温度
总体风力
总体阵风风力
平均空气质量